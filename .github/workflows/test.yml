name: Test
on:
  pull_request:
  schedule:
    - cron: "30 2 * * *"
jobs:
  tests:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
      - run: nix build --show-trace .#docs-jsonModuleMaintainers
      - run: nix fmt -- --ci
      - run: nix run .#home-manager -- init --switch --no-flake
      - run: yes | nix run . -- uninstall
      - name: Run tests
        run: nix build -j auto --show-trace --argstr enableBig false --option allow-import-from-derivation false --reference-lock-file flake.lock "./tests#test-all"
        env:
          GC_INITIAL_HEAP_SIZE: 4294967296
      - name: Run tests (with IFD)
        run: nix build -j auto --show-trace --reference-lock-file flake.lock "./tests#test-all"
        env:
          GC_INITIAL_HEAP_SIZE: 4294967296
